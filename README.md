# React Firebase 家长管理系统

一个基于 React + Firebase 的家长管理系统，包含聊天、请假申请、活动中心等功能模块。

## 📁 项目结构

```
react-firebase-chat/
├── public/                    # 静态资源
│   ├── bg.jpg                # 背景图片
│   ├── avatar.png            # 默认头像
│   └── ...                   # 其他图标资源
├── src/
│   ├── components/           # 组件目录
│   │   ├── activities/      # 活动中心模块
│   │   │   ├── Activities.jsx           # 活动列表（硬编码数据）
│   │   │   ├── ActivityDetail.jsx      # 活动详情页
│   │   │   ├── VotingPanel.jsx         # 投票功能（硬编码数据）
│   │   │   └── activities.css          # 活动样式
│   │   ├── auth/             # 认证模块
│   │   │   ├── Auth.jsx                # 登录/注册组件
│   │   │   ├── AccountsTestPanel.jsx   # Accounts表测试工具
│   │   │   ├── auth.css                # 认证样式
│   │   │   └── accountsTestPanel.css   # 测试面板样式
│   │   ├── chat/             # 聊天模块
│   │   │   ├── Chat.jsx                # 聊天组件
│   │   │   └── chat.css                # 聊天样式
│   │   ├── detail/           # 详情模块
│   │   │   ├── Detail.jsx             # 详情组件
│   │   │   └── detail.css             # 详情样式
│   │   ├── leaveRequest/     # 请假申请模块
│   │   │   ├── pullRequest/           # 请假表单
│   │   │   │   ├── index.jsx          # 请假表单组件
│   │   │   │   └── pullRequest.css    # 表单样式
│   │   │   └── studentsCourseInfo/   # 学生课程信息
│   │   │       ├── index.jsx                  # 课程信息展示
│   │   │       ├── FirebaseTestPanel.jsx      # 学生表测试工具
│   │   │       ├── RequestsTestPanel.jsx      # 请假申请测试工具
│   │   │       ├── studentsCourseInfo.css     # 课程信息样式
│   │   │       └── firebaseTestPanel.css      # 测试面板样式
│   │   ├── list/             # 列表模块
│   │   │   ├── List.jsx              # 列表组件
│   │   │   ├── list.css              # 列表样式
│   │   │   ├── chatList/             # 聊天列表
│   │   │   │   ├── ChatList.jsx      # 聊天列表组件
│   │   │   │   └── chatList.css     # 聊天列表样式
│   │   │   └── userInfo/            # 用户信息
│   │   │       ├── UserInfo.jsx      # 用户信息组件
│   │   │       └── userInfo.css      # 用户信息样式
│   │   └── notification/    # 通知模块
│   │       ├── Notification.jsx      # 通知组件
│   │       └── notification.css      # 通知样式
│   ├── pages/                # 页面组件
│   │   ├── LeaveRequest.jsx          # 请假申请页面（部分硬编码）
│   │   └── leaveRequest.css          # 请假页面样式
│   ├── lib/                  # 工具库
│   │   └── firebase.js       # Firebase 配置
│   ├── App.jsx               # 主应用组件
│   ├── main.jsx              # 应用入口
│   └── index.css             # 全局样式
├── .env                      # 环境变量（需配置 VITE_FIREBASE_API_KEY）
├── package.json              # 项目依赖
├── vite.config.js            # Vite 配置
└── README.md                 # 项目说明文档
```

## 🚀 功能模块

### 1. 认证系统 (`src/components/auth/`)
- **登录/注册**: 家长邮箱登录，注册时绑定学生ID
- **权限控制**: 根据登录用户的学生ID自动过滤数据
- **测试工具**: Accounts表测试面板，可创建测试账号

**硬编码状态**: ❌ 无硬编码，完全使用 Firebase Authentication 和 Firestore

### 2. 请假申请系统 (`src/pages/LeaveRequest.jsx` + `src/components/leaveRequest/`)
- **课程信息展示**: 显示学生课程、即将开始的课程
- **请假申请**: 填写请假原因、选择补课时间
- **申请记录**: 查看历史请假申请及审核状态
- **测试工具**: 
  - 学生表测试工具（FirebaseTestPanel）
  - 请假申请测试工具（RequestsTestPanel）

**硬编码状态**: ⚠️ **部分硬编码**
- `LeaveRequest.jsx`: 课程数据（lessons数组）使用硬编码
- 学生数据从 Firebase `students` 集合加载 ✅
- 请假申请数据从 Firebase `requests` 集合加载 ✅

### 3. 活动中心 (`src/components/activities/`)
- **活动列表**: 图片+文字展示，支持状态筛选
- **活动详情**: 查看活动介绍、规则、安排、奖项
- **投票功能**: 为进行中的活动投票支持表现突出的小朋友

**硬编码状态**: ⚠️ **完全硬编码**
- `Activities.jsx`: 活动数据完全硬编码（4个示例活动）
- `VotingPanel.jsx`: 投票数据硬编码
- 后续需要迁移到 Firebase `activities` 集合

### 4. 聊天系统 (`src/components/chat/`, `src/components/list/`)
- **聊天界面**: 实时聊天功能
- **用户列表**: 显示联系人列表
- **用户信息**: 显示用户详细信息

**硬编码状态**: ⚠️ **可能硬编码**（需要确认）

## 🔥 Firebase 数据库结构

### Collections（集合）

#### 1. `students` - 学生表
```javascript
{
  id: "STUDENT_001",  // 文档ID
  name: "学生姓名",
  level: "年级/级别",
  age: "年龄",
  class: "班级",
  parentName: "家长姓名",
  parentPhone: "家长电话",
  enrollmentDate: "入学日期",
  // ... 其他字段
}
```

#### 2. `accounts` - 账号表
```javascript
{
  id: "用户UID",  // 文档ID（使用Firebase Auth的UID）
  email: "parent@example.com",
  studentId: "STUDENT_001",
  role: "parent",
  createdAt: "2025-01-01T00:00:00.000Z",
  uid: "用户UID"  // 冗余字段
}
```

**注意**: 账号数据同时保存在两个位置：
- `accounts/{uid}` - 使用UID作为文档ID（主要查询位置）
- `accounts` 集合 - 使用自动生成的文档ID（兼容旧数据）

#### 3. `requests` - 请假申请表
```javascript
{
  id: "自动生成ID",
  studentId: "STUDENT_001",
  studentName: "学生姓名",
  lesson: {
    id: 3,
    name: "课程名称",
    dateTime: "2025-12-20T12:00-14:00",
    timeSlot: "SAT 12:00 - 14:00"
  },
  courseCode: "SPEC_C001",
  courseName: "演讲课程",
  reason: "illness",
  description: "请假说明",
  makeupOption: "specific_time",
  selectedTimeSlot: { /* 补课时间信息 */ },
  submitTime: "2025-12-17 14:30:00",
  status: "pending",  // pending, approved, rejected
  reviewTime: "2025-12-17 15:20:00",
  reviewNote: "审核备注"
}
```

#### 4. `activities` - 活动表（待实现）
**当前状态**: ⚠️ 使用硬编码数据，尚未连接数据库

```javascript
{
  id: "自动生成ID",
  title: "活动标题",
  description: "活动描述",
  image: "图片URL",
  status: "ongoing",  // upcoming, ongoing, ended
  startDate: "2025-03-01",
  endDate: "2025-03-31",
  location: "活动地点",
  participants: 25,
  organizer: "组织者",
  category: "比赛",
  details: {
    introduction: "活动介绍",
    rules: ["规则1", "规则2"],
    prizes: ["奖项1", "奖项2"],
    schedule: ["安排1", "安排2"]
  },
  candidates: [
    { id: 1, name: "候选人", votes: 15, avatar: "头像URL" }
  ]
}
```

#### 5. `messages` - 聊天消息表（待实现）
**当前状态**: ⚠️ 使用硬编码数据，尚未连接数据库

```javascript
{
  id: "自动生成ID",
  senderId: "用户ID",
  receiverId: "接收者ID",
  content: "消息内容",
  avatar: "头像URL",
  time: "时间戳",
  timestamp: "2025-01-01T00:00:00.000Z"
}
```

#### 6. `courses` / `lessons` - 课程表（待实现）
**当前状态**: ⚠️ 使用硬编码数据，尚未连接数据库

```javascript
{
  id: "课程ID",
  courseCode: "SPEC_C001",
  courseName: "演讲课程",
  studentId: "STUDENT_001",
  lessons: [
    {
      id: 1,
      name: "课程名称",
      dateTime: "2025-12-06T12:00-14:00",
      timeSlot: "SAT 12:00 - 14:00",
      completed: true
    }
  ]
}
```

#### 7. `timeSlots` - 补课时间段表（待实现）
**当前状态**: ⚠️ 使用硬编码数据，尚未连接数据库

```javascript
{
  id: "时间段ID",
  date: "2025-12-21",
  dateDisplay: "2025年12月21日",
  day: "周六",
  time: "12:00 - 14:00",
  currentStudents: 8,
  maxStudents: 10,
  available: true
}
```

## 🛠️ 技术栈

- **前端框架**: React 18.2.0
- **构建工具**: Vite 5.2.0
- **后端服务**: Firebase
  - Firebase Authentication（邮箱/密码认证）
  - Cloud Firestore（数据库）
- **UI库**: 原生 CSS（自定义样式）
- **其他依赖**: emoji-picker-react

## 📦 安装与运行

### 1. 安装依赖
```bash
npm install
```

### 2. 配置环境变量
创建 `.env` 文件：
```env
VITE_FIREBASE_API_KEY=your_firebase_api_key
```

### 3. 配置 Firebase
1. 在 Firebase Console 中创建项目
2. 启用 Authentication - 邮箱/密码登录
3. 创建 Firestore 数据库
4. 配置 Firestore 安全规则（开发阶段可设置为测试模式）

### 4. 运行开发服务器
```bash
npm run dev
```

### 5. 构建生产版本
```bash
npm run build
```

## 🔐 认证流程

### 注册流程
1. 用户填写邮箱、密码、学生ID
2. 系统验证学生ID是否存在于 `students` 集合
3. 创建 Firebase Authentication 账号
4. 在 `accounts` 集合中创建账号记录（使用UID作为文档ID）
5. 自动登录

### 登录流程
1. 用户输入邮箱和密码
2. Firebase Authentication 验证
3. 从 `accounts` 集合获取账号信息（包含学生ID）
4. 根据学生ID过滤显示数据

### 权限控制
- 登录后只能查看绑定学生ID的数据
- 请假申请自动关联到登录用户的学生ID
- 学生信息自动加载对应学生的数据

## 🧪 测试工具

### 1. 学生表测试工具
- 位置: 请假申请页面 → "显示学生表测试"
- 功能:
  - 获取所有学生列表
  - 查看学生详细信息
  - 显示所有字段及类型

### 2. 请假申请测试工具
- 位置: 请假申请页面 → "显示请假申请测试"
- 功能:
  - 获取所有请假申请
  - 按学生ID查询
  - 创建测试数据（待审核/已通过/已拒绝）

### 3. 账号测试工具
- 位置: 请假申请页面 → "显示账号测试"
- 功能:
  - 获取所有账号
  - 批量创建测试账号
  - 单个创建测试账号

## ⚠️ 硬编码数据清单

### 需要迁移到数据库的模块：

#### 1. 活动中心 (`src/components/activities/`)
- ✅ **Activities.jsx**: 活动列表数据（4个示例活动）
- ✅ **VotingPanel.jsx**: 投票候选人和票数
- **迁移计划**: 创建 `activities` 集合，迁移活动数据

#### 2. 请假申请 (`src/pages/LeaveRequest.jsx`)
- ✅ **课程数据**: `lessons` 数组（12节课程）- **硬编码**
- ✅ **补课时间段**: `makeupTimeSlots` 数组 - **硬编码**
- **迁移计划**: 
  - 创建 `courses` 或 `lessons` 集合存储课程数据
  - 创建 `timeSlots` 集合存储补课时间段
- **已连接数据库**:
  - ✅ 学生数据从 `students` 集合加载
  - ✅ 请假申请从 `requests` 集合加载

#### 3. 聊天系统 (`src/components/chat/`, `src/components/list/`)
- **硬编码状态**: ⚠️ **完全硬编码**
- `Chat.jsx`: 聊天消息数据硬编码（示例消息）
- `currentUserId`: 当前用户ID硬编码为 "user_123"
- **迁移计划**: 创建 `messages` 集合，实现实时聊天功能

## 📝 待办事项

### 数据库迁移
- [ ] 将活动数据迁移到 Firebase `activities` 集合
  - [ ] 迁移活动基本信息
  - [ ] 迁移活动详情（介绍、规则、安排、奖项）
  - [ ] 迁移投票候选人数据
  - [ ] 实现投票实时同步
- [ ] 将课程数据迁移到 Firebase `courses` 或 `lessons` 集合
  - [ ] 迁移课程列表（12节课程）
  - [ ] 迁移课程进度状态
  - [ ] 实现课程数据的动态加载
- [ ] 将补课时间段迁移到 Firebase `timeSlots` 集合
  - [ ] 存储可用时间段
  - [ ] 实现时间段实时更新
- [ ] 将聊天消息迁移到 Firebase `messages` 集合
  - [ ] 实现实时消息同步
  - [ ] 实现消息历史记录

### 功能增强
- [ ] 添加活动管理后台（管理员功能）
- [ ] 实现活动图片上传功能
- [ ] 添加课程管理功能
- [ ] 完善投票防刷机制

### 安全与优化
- [ ] 配置 Firestore 安全规则
- [ ] 实现投票防重复机制（使用用户ID）
- [ ] 添加数据验证规则
- [ ] 优化查询性能（添加索引）

## 🔧 Firebase 配置要求

### Authentication
- ✅ 启用邮箱/密码登录方式
- ✅ 配置邮箱验证（可选）

### Firestore
- ✅ 已创建集合：
  - `students` - 学生信息
  - `accounts` - 账号信息
  - `requests` - 请假申请
- ⚠️ 待创建集合：
  - `activities` - 活动信息
  - `courses` / `lessons` - 课程信息
  - `timeSlots` - 补课时间段
  - `messages` - 聊天消息

### 索引要求
- `requests` 集合需要创建复合索引：
  - `studentId` (Ascending) + `submitTime` (Descending)

## 📄 许可证

本项目为私有项目。

## 👥 开发者

如有问题或建议，请联系开发团队。
